// manager guest order module

const db = require('../db');
const authController = require('./auth.controller');

class OrderController{
  index(req, res, next){

  }
  post(req, res, next){
    
  }
}

module.exports = new OrderController();